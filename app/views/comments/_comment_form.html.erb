<% unless current_user.is_a? GuestUser %>
	<%= form_for @comment, url: '#' do |f| %>
	  <div class="form-group">
	    <%= f.label "Comentarios:" %>
			<hr>
	    <%= f.text_area :content, class: 'form-control'%>
	  </div>
	  <%= f.submit 'Añadir comentario', class: 'btn btn-outline-primary'%>
	<% end %>
<% end %>